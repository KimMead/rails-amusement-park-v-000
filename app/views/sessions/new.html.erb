<h1>Sign In</h1>
<%= form_tag("/signin", method: "post") do %>
  
  <p>Select a User:</p>
  <%= select_tag "user_name", options_from_collection_for_select(@users, "user_name", "user_name") %>
  <p>Password: <%= password_field_tag :password %></p>
  <%= submit_tag "Sign In" %>
<% end %>